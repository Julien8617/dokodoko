# Copier en .env (ignoré par git) et compléter avec les valeurs du projet
# Supabase (Dashboard > Project Settings > API). La clé anon est publique
# par nature : le RLS est la seule barrière (spec v2 §3).
VITE_SUPABASE_URL=https://xxxxxxxxxxxx.supabase.co
VITE_SUPABASE_ANON_KEY=
